# OpenAI API Key - Get this from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# Gmail API credentials
# These are typically not needed in .env if using OAuth flow with credentials.json
# GMAIL_CLIENT_ID=your-client-id
# GMAIL_CLIENT_SECRET=your-client-secret

# Optional: Number of emails to process (default is 5)
# MAX_EMAILS=5

# Optional: Model to use for summarization (default is gpt-3.5-turbo)
# OPENAI_MODEL=gpt-3.5-turbo 